# wolfi-common-aws-lambda-python312

Builds a Python tooling base with `python3`, `uv`, AWS CLI, Terraform, OpenBao, Docker CLI, Java, curl, wget, git, jq, zip, and unzip.

```sh
make -C examples/wolfi-common-aws-lambda-python312 all
```
