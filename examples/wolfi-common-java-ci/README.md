# wolfi-common-java-ci

Builds a Java CI base with OpenJDK, Maven, native build tools, Docker CLI, AWS CLI, OpenBao, git, curl, jq, tar, and unzip.

```sh
make -C examples/wolfi-common-java-ci all
```
