# wolfi-common-docker-toolbox

Builds a CI toolbox with Docker CLI/buildx/compose, AWS CLI, Terraform, OpenBao, Helm, kubectl, argo rollouts, Flux, buf, uv, yamllint, curl, wget, git, jq, yq, rsync, make, zip, and unzip.

```sh
make -C examples/wolfi-common-docker-toolbox all
```
