# Wolfi Node Runtime

Builds a chiseled Wolfi Node.js runtime image with CA certificates and the
minimal Node package.

```sh
make -C examples/wolfi-node-runtime build
make -C examples/wolfi-node-runtime test
make -C examples/wolfi-node-runtime size
make -C examples/wolfi-node-runtime scan
```
