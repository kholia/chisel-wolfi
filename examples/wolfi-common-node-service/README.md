# wolfi-common-node-service

Builds the Node.js UI base plus `su-exec` for runtime user switching. The source directory name is misspelled as `node_service`; this example uses the corrected `hierarchy` spelling.

```sh
make -C examples/wolfi-common-node-service all
```
