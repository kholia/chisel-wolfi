# wolfi-common-go-service-runtime

Builds a small Go service runtime base with CA certificates, BusyBox shell, and `grpc-health-probe`.

```sh
make -C examples/wolfi-common-go-service-runtime all
```
