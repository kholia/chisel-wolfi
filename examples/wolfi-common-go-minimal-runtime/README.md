# wolfi-common-go-minimal-runtime

Uses public Wolfi slices for the same runtime role as the distroless base: CA certificates, BusyBox shell, and `grpc-health-probe`.

```sh
make -C examples/wolfi-common-go-minimal-runtime all
```
