# wolfi-common-node-spa

Builds the shared Node.js UI build base with npm, yarn, pnpm, Python, native build tools, git, curl, and archive utilities.

```sh
make -C examples/wolfi-common-node-spa all
```
