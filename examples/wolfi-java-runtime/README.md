# Wolfi Java Runtime

Builds a chiseled Wolfi OpenJDK runtime image with CA certificates and Java
trust-store data.

```sh
make -C examples/wolfi-java-runtime build
make -C examples/wolfi-java-runtime test
make -C examples/wolfi-java-runtime size
make -C examples/wolfi-java-runtime scan
```
