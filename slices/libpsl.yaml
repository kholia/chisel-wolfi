package: libpsl

slices:
  libs:
    essential:
      glibc_runtime:
      libidn2_libs:
      libunistring_libs:
    contents:
      /usr/lib/libpsl.so.5:
      /usr/lib/libpsl.so.5.*:

  sbom:
    contents:
      /var/lib/db/sbom/libpsl-*.spdx.json:
