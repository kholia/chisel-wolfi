package: nats-server

slices:
  bins:
    contents:
      /usr/bin/nats-server:

  config:
    contents:
      /etc/nats/nats-server.conf:

  sbom:
    contents:
      /var/lib/db/sbom/nats-server-*.spdx.json:
