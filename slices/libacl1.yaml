package: libacl1

slices:
  libs:
    essential:
      glibc_runtime:
    contents:
      /usr/lib/libacl.so.1:
      /usr/lib/libacl.so.1.*:

  sbom:
    contents:
      /var/lib/db/sbom/libacl1-*.spdx.json:
