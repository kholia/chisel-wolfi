package: fluent-bit-5.0

slices:
  bins:
    essential:
      fluent-bit-5.0_libs:
      libcrypto3_libs:
      libjemalloc2_libs:
      libnghttp2-14_libs:
      libpq-18_libs:
      librdkafka_libs:
      libssl3_libs:
      libsystemd_libs:
      luajit_runtime:
      msgpack-c_libs:
      sqlite-libs_libs:
      yaml_libs:
    contents:
      /usr/bin/fluent-bit:

  libs:
    essential:
      glibc_runtime:
    contents:
      /usr/lib/fluent-bit/libfluent-bit.so:

  config:
    contents:
      /fluent-bit/schema.json:
      /fluent-bit/etc/**:

  systemd:
    contents:
      /usr/lib/systemd/system/fluent-bit.service:

  sbom:
    contents:
      /var/lib/db/sbom/fluent-bit-5.0-*.spdx.json:
