package: libexpat1

slices:
  libs:
    essential:
      glibc_runtime:
    contents:
      /usr/lib/libexpat.so.1:
      /usr/lib/libexpat.so.1.*:

  sbom:
    contents:
      /var/lib/db/sbom/libexpat1-*.spdx.json:
