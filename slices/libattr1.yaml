package: libattr1

slices:
  libs:
    essential:
      glibc_runtime:
    contents:
      /usr/lib/libattr.so.1:
      /usr/lib/libattr.so.1.*:

  sbom:
    contents:
      /var/lib/db/sbom/libattr1-*.spdx.json:
