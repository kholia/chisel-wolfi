package: libidn2

slices:
  libs:
    essential:
      glibc_runtime:
    contents:
      /usr/lib/libidn2.so.0:
      /usr/lib/libidn2.so.0.*:

  sbom:
    contents:
      /var/lib/db/sbom/libidn2-*.spdx.json:
