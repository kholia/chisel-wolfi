package: keyutils-libs

slices:
  libs:
    essential:
      glibc_runtime:
    contents:
      /usr/lib/libkeyutils.so.1:
      /usr/lib/libkeyutils.so.1.*:

  sbom:
    contents:
      /var/lib/db/sbom/keyutils-libs-*.spdx.json:
