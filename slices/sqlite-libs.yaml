package: sqlite-libs

slices:
  libs:
    essential:
      glibc_runtime:
    contents:
      /usr/lib/libsqlite3.so.0:
      /usr/lib/libsqlite3.so.*:

  sbom:
    contents:
      /var/lib/db/sbom/sqlite-libs-*.spdx.json:
