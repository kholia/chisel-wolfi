package: freetype

slices:
  libs:
    essential:
      glibc_runtime:
      libbrotlidec1_libs:
      libpng_libs:
      zlib_libs:
    contents:
      /usr/lib/libfreetype.so.6:
      /usr/lib/libfreetype.so.6.*:
      /var/lib/db/sbom/freetype-*.spdx.json:
